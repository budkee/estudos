/**
 * Exercício 3 - Função exponencial com a base e expoente como parâmetros.
 * Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.
 */

function funcaoExponencial(b, e) {
    expo = b ** e
    return expo
}

console.log(funcaoExponencial(2,3))