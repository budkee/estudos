Dado este trecho de código para extrair e armazenar os dados em um dicionário de atores: 

def remove_aspas_duplas(valor):
    return valor.strip('"')


# Criando o dicionário de atores
atores = {}

# Abrindo o dataset e extraindo os dados
with open('actors.csv', 'r') as arquivo:

    linhas = arquivo.readlines()
    chaves = linhas[0].strip().split(',')
    atores = dict.fromkeys(chaves)
    #print(chaves)
    #print(atores)

    for registro in linhas[1:]:
        
        # Armazene o valor
        valores = registro.strip().split(',')

        # Remova as aspas duplas dos valores usando a função
        valores = [remove_aspas_duplas(valor) for valor in valores]
        
        # Preencha o dicionário de atores com os valores correspondentes
        for i, chave in enumerate(chaves):
            atores[chave] = valores[i]

        print(atores)

Dado a seguinte linha do arquivo csv, contendo aspas duplas e uma vírgula no nome do ator: 

"Robert Downey, Jr.",3947.30 ,53,74.50 ,The Avengers,623.40 

Realize as devidas correções para que o código extraia e armazene corretamente os valores  da linha. A saída correta deverá ser no seguinte formato:

{'Actor': 'Robert Downey Jr.', 'Total Gross': '3947.30', 'Number of Movies': '53', 'Average per Movie':  '74.50', '#1 Movie': 'The Avengers', 'Gross': '623.40'}
