-- Apaga todos os registros de uma tabela, mas mantém a estrutura
TRUNCATE `tabela`
    -- RESTART IDENTITY: Reinicia a contagem do ID para 1
    RESTART IDENTITY;

-- Apaga todos os registros de uma tabela e a estrutura
DROP `tabela`

-- Apaga uma tabela
DROP TABLE `tabela`

-- Apaga um banco de dados
DROP DATABASE `banco`

-- Apaga um usuário
DROP USER `usuario`

