#include <stdio.h> 
// Registro
typedef struct {

    int numero;
    int pontos;

} capivara

// Funçoes
void ordenacaoDecrescente(int n; int v[]) {

    for (int i = 0, j = 0; i, j < n; i++, j++) {

        if (/* Caso mais fundamental */) {

        } else {
            // Caso repetitivo
        }
    }

}
// Inicio
int maint(void) {

    // Declaração das variaveis
    int n, num_ultrapassagem; 

    // Leitura do tamanho
    scanf("%d", n);

    // Declaração 
    capivara competidoras[n];

    // Preenchimento do vetor
    for (int i = 0; i < n; i++) {

        competidoras.numero[i] += 1;
        competidoras.pontos[i] = 0;
         
    }

    // Leitura do arquivo
    while (scanf("%d", num_ultrapassagem) != EOF) {

        // Ultrapassagem
        if (num_ultrapassagem == competidoras.numero[i]) {
            troca();
            // Troque a posição e acrescente um ponto
        }
        
    }


}